package myfirstassignment;

public class SleepingIn {

	public static boolean willISleepIn(boolean workedOvertime, int hoursWorked, double milligramsOfCaffeine, boolean headache)   {
		// TODO Auto-generated method stub
		if ( (workedOvertime = true) && (hoursWorked >= 11) && (milligramsOfCaffeine >=300.0) && (headache = true) ) {
		
		}
		return true;
	}
	
}
